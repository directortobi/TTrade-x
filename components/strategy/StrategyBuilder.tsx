import React from 'react';
import ComingSoonPage from '../../pages/ComingSoonPage';

const StrategyBuilder: React.FC = () => {
  return (
    <ComingSoonPage 
      title="Strategy Builder"
      description="This advanced feature will allow you to build, backtest, and deploy your own custom trading strategies using a visual, no-code interface. Stay tuned for updates!"
    />
  );
};

export default StrategyBuilder;
